<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.digit2020.donirajsreka">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".PlatiSmetkaActivity"></activity>
        <activity android:name=".VolontirajActivity" />
        <activity android:name=".TehnikaActivity" />
        <activity android:name=".LekoviActivity" />
        <activity android:name=".OblekaActivity" />
        <activity android:name=".HranaActivity" />
        <activity android:name=".ZaNasActivity" />
        <activity android:name=".KontaktActivity" />
        <activity android:name=".StaristikaActivity" />
        <activity android:name=".PomogniActivity" />
        <activity android:name=".DonirajActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>